# Convert an object to a staticPath object

This function is used to convert an object to a `staticPath` object. It
is primarily used internally, but can also be used by users to ensure
that an object is a `staticPath` object.

## Usage

``` r
as.staticPath(path)
```

## Arguments

- path:

  The object to convert.
